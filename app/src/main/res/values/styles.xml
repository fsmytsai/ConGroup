<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimary">@color/colorOrange</item>-->
        <item name="colorPrimaryDark">@color/colorOrange</item>
    </style>

    <style name="SwipableLayoutTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/colorOrange</item>

        <!--<item name="windowNoTitle">false</item>-->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--<item name="android:windowNoTitle">false</item>-->
        <item name="windowActionBar">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <!--<item name="tabMaxWidth">@dimen/design_tab_max_width</item>-->
        <item name="tabMaxWidth">700dp</item>
        <item name="tabSelectedTextColor">#e5004f</item>
        <item name="tabTextColor">#FFFFFF</item>
        <item name="tabIndicatorColor">#e5004f</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabPaddingStart">12dp</item>
        <item name="tabPaddingEnd">12dp</item>
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="background">@android:color/black</item>
        <item name="tabTextAppearance">@style/MyCustomTextAppearance</item>
    </style>
    #tabTextAppearance引用到下方style

    <style name="MyCustomTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
    </style>
    #Tab的title是否全大寫，如果Tab上是放icon就設false

</resources>
